# Copy this file to `.env` and fill in your Supabase project values.
# Do NOT commit your real `.env` to version control.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-anon-key

# After creating `.env`, restart the dev server.
# For production, set these values in your hosting environment's secret store.
